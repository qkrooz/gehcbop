import React from "react";
const QaTestIndex = React.memo(() => {
  return (
    <div
      style={{
        flex: 1,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <span style={{ fontSize: "3em", color: "gray" }}>
        App under development
      </span>
    </div>
  );
});
export default QaTestIndex;
